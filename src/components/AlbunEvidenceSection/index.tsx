import React from 'react';

const AlbunEvidenceSection: React.FC = ({ children }) => {
  return (
    <div className="h-full max-h-full flex justify-center items-center z-10">
      {children}
    </div>
  );
};

export default AlbunEvidenceSection;
